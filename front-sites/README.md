# front-sites

Front-end applications for the online school multi-tenant platform.

## Projects

- **central-app** — React (Vite) admin/central app. Run: `cd central-app && npm install && npm run dev`. Docker: built via root `docker compose` (service `central-app`).
- *(Other sites can be added here later.)*
